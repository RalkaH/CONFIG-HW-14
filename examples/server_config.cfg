REM Конфигурация веб-сервера

const port = 8080.0;
const timeout = 30.0;
const max_workers = 4.0;

(comment
Основные настройки сервера
Порт и таймауты
)

const connections = !{port + 10.0};
const worker_timeout = !{timeout + 10.0};

@"localhost"
!{port}
!{pow(max_workers, 2.0)}

[
    @"GET"
    @"POST"
    @"PUT"
    @"DELETE"
]

!{ord(@"H")}
